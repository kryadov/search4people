# Copy this file to .env and fill in the values as needed
# Only one LLM provider is required. If none are provided, the app will use a deterministic fallback.

# --- OpenAI ---
# Required for using OpenAI models via the openai SDK
OPENAI_API_KEY=
# Optional, default: gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# --- Google Gemini ---
# Required for using Google Generative AI (Gemini)
GEMINI_API_KEY=
# Optional, default: gemini-1.5-flash
GEMINI_MODEL=gemini-1.5-flash

# --- Ollama (local) ---
# If set, will use the local Ollama server for generation
OLLAMA_MODEL=
# Optional, default: http://localhost:11434
OLLAMA_HOST=http://localhost:11434

# --- Server ---
# Port for uvicorn when running `python -m src.app` directly (default: 8000)
PORT=8000
